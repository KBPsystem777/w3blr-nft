# w3blr smart contracts

Try running some of the following tasks:

```shell
npx hardhat accounts
npx hardhat compile
npx hardhat node run --network rinkeby scripts/deployNFT.js
npx hardhat clean
npx hardhat test
npx hardhat help
```
